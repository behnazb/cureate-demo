import { Controller } from "@hotwired/stimulus"

// sidebar_controller — placeholder for any future sidebar-local interactions.
// Currently the sidebar is fully ERB-driven; active highlighting is determined server-side.
export default class extends Controller {}
