import { Controller } from "@hotwired/stimulus"

// vendor_detail_controller — currently nothing to wire; vendor detail page is
// fully ERB-driven. Reserved for future interactions like inline editing.
export default class extends Controller {}
