import { Controller } from "@hotwired/stimulus"

// product_card_controller — wrapper hook for the product card link.
// Currently no logic needed; the add-to-cart button inside handles its own clicks.
export default class extends Controller {}
