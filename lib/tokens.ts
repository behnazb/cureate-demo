const scale = 1.25 // 125% — change this one number to rescale everything

export const t = {
  // Font sizes (base * scale)
  text10: `${Math.round(10 * scale)}px`,
  text11: `${Math.round(11 * scale)}px`,
  text12: `${Math.round(12 * scale)}px`,
  text13: `${Math.round(13 * scale)}px`,
  text14: `${Math.round(14 * scale)}px`,
  text16: `${Math.round(16 * scale)}px`,
  text18: `${Math.round(18 * scale)}px`,
  text20: `${Math.round(20 * scale)}px`,
  text24: `${Math.round(24 * scale)}px`,
  text28: `${Math.round(28 * scale)}px`,
  text32: `${Math.round(32 * scale)}px`,
  text36: `${Math.round(36 * scale)}px`,
  text40: `${Math.round(40 * scale)}px`,

  // Heights
  h24:  `${Math.round(24  * scale)}px`,
  h30:  `${Math.round(30  * scale)}px`,
  h36:  `${Math.round(36  * scale)}px`,
  h40:  `${Math.round(40  * scale)}px`,
  h48:  `${Math.round(48  * scale)}px`,
  h120: `${Math.round(120 * scale)}px`,
  h140: `${Math.round(140 * scale)}px`,
  h160: `${Math.round(160 * scale)}px`,
  h320: `${Math.round(320 * scale)}px`,

  // Widths
  w30:  `${Math.round(30  * scale)}px`,
  w40:  `${Math.round(40  * scale)}px`,
  w44:  `${Math.round(44  * scale)}px`,
  w48:  `${Math.round(48  * scale)}px`,
  w65:  `${Math.round(65  * scale)}px`,
  w109: `${Math.round(109 * scale)}px`,
  w126: `${Math.round(126 * scale)}px`,
  w160: `${Math.round(160 * scale)}px`,
  w163: `${Math.round(163 * scale)}px`,
  w180: `${Math.round(180 * scale)}px`,
  w200: `${Math.round(200 * scale)}px`,
  w220: `${Math.round(220 * scale)}px`,
  w331: `${Math.round(331 * scale)}px`,

  // Spacing/padding
  p6:  `${Math.round(6  * scale)}px`,
  p8:  `${Math.round(8  * scale)}px`,
  p12: `${Math.round(12 * scale)}px`,
  p16: `${Math.round(16 * scale)}px`,
  p24: `${Math.round(24 * scale)}px`,
  p36: `${Math.round(36 * scale)}px`,

  // Border radius
  pill:  '9999px',
  card:  `${Math.round(12 * scale)}px`,
  thumb: `${Math.round(8  * scale)}px`,

  // Icon sizes
  icon12: `${Math.round(12 * scale)}px`,
  icon16: `${Math.round(16 * scale)}px`,
  icon18: `${Math.round(18 * scale)}px`,
  icon22: `${Math.round(22 * scale)}px`,
}
